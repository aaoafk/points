<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Fast Points" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
		<%= turbo_refreshes_with(method: :morph, scroll: :preserve) %>
		<%= yield :head %>

		<!-- Add these lines to include the Google Fonts -->
		<link href="https://fonts.googleapis.com/css2?family=Honk&family=Bungee+Spice&display=swap" rel="stylesheet">
		<!-- Add this style tag for a game-like background -->
		<style>
		 body {
				 background-color: #1a1a2e;
				 background-image: radial-gradient(#ffffff33 1px, transparent 1px);
				 background-size: 20px 20px;
		 }
		</style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
